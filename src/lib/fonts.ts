// Font CSS variables applied via globals.css
// In production, add Google Fonts link for Playfair Display + DM Sans
// For now, using system font fallbacks that will be overridden when
// Google Fonts loads via the <link> tag in layout.tsx

export const fontVariableClasses = "font-sans";
